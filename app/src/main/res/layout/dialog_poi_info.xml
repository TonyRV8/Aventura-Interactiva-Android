<?xml version="1.0" encoding="utf-8"?>
<!-- Diálogo para mostrar información de puntos de interés -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="24dp"
    android:background="#FFFFFF">

    <!-- Título del POI -->
    <TextView
        android:id="@+id/dialog_poi_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Título"
        android:textSize="24sp"
        android:textStyle="bold"
        android:textColor="#000000"
        android:gravity="center"
        android:layout_marginBottom="16dp" />

    <!-- Descripción -->
    <TextView
        android:id="@+id/dialog_poi_description"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Descripción del lugar"
        android:textSize="16sp"
        android:textColor="#333333"
        android:layout_marginBottom="16dp"
        android:lineSpacingExtra="4dp" />

    <!-- Separador -->
    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="#CCCCCC"
        android:layout_marginVertical="8dp" />

    <!-- Dato curioso -->
    <TextView
        android:id="@+id/dialog_poi_fun_fact"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="💡 Dato curioso"
        android:textSize="14sp"
        android:textColor="#666666"
        android:textStyle="italic"
        android:lineSpacingExtra="4dp" />

</LinearLayout>